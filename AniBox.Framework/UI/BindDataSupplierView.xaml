<Window x:Class="AniBox.Framework.UI.BindDataSupplierView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Height="300" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <ListBox x:Name="lstFields" Grid.Row="0" DisplayMemberPath="FieldName" 
                 SelectionChanged="lstFields_SelectionChanged"></ListBox>
        <Button Grid.Row="1" x:Name="btnOK" Width="80" Height="25" Margin="0,10,0,0" Click="btnOK_Click" >OK</Button>
    </Grid>
</Window>
