<Window x:Class="AniBox.Framework.UI.AddFieldView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:pg="clr-namespace:System.Windows.Controls.WpfPropertyGrid;assembly=System.Windows.Controls.WpfPropertyGrid"
             mc:Ignorable="d" Title="添加字段"
             d:DesignHeight="300" d:DesignWidth="300" Width="650" Height="480"  WindowStartupLocation="CenterOwner" Closed="Window_Closed">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBlock>Source example</TextBlock>
            <TextBox x:Name="txtFieldsSource" Grid.Row="0" Height="60" TextWrapping="Wrap"></TextBox>

            <TextBlock VerticalAlignment="Center" Margin="0,10,0,0">Field Name</TextBlock>
            <TextBox x:Name="txtFieldName" Height="25"></TextBox>

            <TextBlock VerticalAlignment="Center" Margin="0,10,0,0">Process Types</TextBlock>
            <ComboBox x:Name="comboProcessType" Height="25"
                      SelectionChanged="comboProcessType_SelectionChanged" 
                      ItemsSource="{Binding ProcessTypes}" DisplayMemberPath="Name">

            </ComboBox>
        </StackPanel>
        <Grid Grid.Row="1" Margin="0,5,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition Width="200"></ColumnDefinition>
                <ColumnDefinition Width="60"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Border BorderBrush="Gray" BorderThickness="1">
                    <pg:PropertyGrid x:Name="lstProperties" PropertyFilterVisibility="Collapsed" 
                        ></pg:PropertyGrid>
                </Border>
            </Grid>
            <Grid Grid.Column="1" Margin="10,0,0,0">
                <Border BorderBrush="Gray" BorderThickness="1">
                    <TextBox x:Name="txtProcessResult" TextWrapping="Wrap"></TextBox>
                </Border>
            </Grid>
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>

                <Button x:Name="btnTestProcessor" Grid.Row="0" Height="25" Margin="10" Click="btnTestProcessor_Click">Test</Button>
                <Button x:Name="btnAddProcessor" Grid.Row="1" Height="25" Margin="10" Click="btnAddProcessor_Click">Add</Button>
            </Grid>
            
        </Grid>
        <Grid Grid.Row="2" Margin="0,5,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition Width="60"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <DataGrid x:Name="dgFieldsProcessors" Grid.Column="0" ItemsSource="{Binding ProcessEntries}" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Width="150" Header="Type Name" Binding="{Binding TypeName}" ></DataGridTextColumn>
                    <DataGridTextColumn Width="200" Header="Setting" Binding="{Binding Setting}"></DataGridTextColumn>
                    <DataGridTextColumn Width="200" Header="output" Binding="{Binding Output}"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>

                </Grid.RowDefinitions>
                <Button Grid.Row="0" Height="20" Width="50">Remove</Button>

            </Grid>
        </Grid>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="Ok" Width="100" Click="Ok_Click" Height="25">OK</Button>
            <Button x:Name="Cancel" Width="100" Click="Cancel_Click" Margin="10,0,0,0" Height="25">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>
